<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Declared explicitly rather than relying on the platform default. The UI
      paints dark backgrounds with white text, so inheriting a light default
      theme would render white-on-white. DeviceDefault is used instead of an
      AppCompat theme because no AppCompat class is used anywhere in the app.
    -->
    <style name="Theme.ShuddhaType" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:textColorHint">@color/hint</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:navigationBarColor">@color/bg</item>
    </style>
</resources>
